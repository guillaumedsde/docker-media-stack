db_config:
  type: postgresql
  host: ${CROWDSEC_DB_HOST}
  port: 5432
  db_name: crowdsec
  user: crowdsec
  password: ${CROWDSEC_DB_PASSWORD}
  sslmode: disable

api:
  client:
    online_client: {}

prometheus:
  enabled: false
