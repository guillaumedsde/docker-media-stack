apiVersion: 1
prune: true

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Access mode - proxy (server in the UI) or direct (browser in the UI).
    url: http://prometheus:9090
    uid: prom_datasource
    version: 1
    isDefault: true
    editable: false
